import PropTypes from 'prop-types';

const Blog = () => {
    return (
        <div>
            <h2>Blog:</h2>
        </div>
    );
};
Blog.propTypes ={
    Blog: PropTypes.object.isRequired
}
export default Blog;